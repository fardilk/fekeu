# Example production environment file for be03 (copy to .env.production and edit)

APP_ENV=prod
LOG_LEVEL=info
SERVER_PORT=8080
GIN_MODE=release

# --- Security ---
# Generate a strong random secret, e.g.: openssl rand -hex 48
JWT_SECRET=CHANGE_ME_LONG_RANDOM_SECRET

# --- Database (choose either DSN or parts) ---
DB_HOST=postgres
DB_PORT=5432
DB_USER=keu_app
DB_PASSWORD=CHANGE_ME_DB_PASSWORD
DB_NAME=cat_keu
# If using SSL with managed provider adjust sslmode (require / verify-full)
DB_DSN=postgres://keu_app:CHANGE_ME_DB_PASSWORD@postgres:5432/cat_keu?sslmode=disable

# --- CORS / Public URL ---
PUBLIC_BASE_URL=https://keu.fardil.com
ALLOW_ORIGINS=https://keu.fardil.com

# --- File storage (local path inside container) ---
UPLOAD_DIR=/app/public

# --- Optional OCR tuning (placeholder) ---
# OCR_LANG=eng
# OCR_MIN_CONF=0.15

# --- Build metadata (optional) ---
DOCKER_IMAGE=keu-app
DOCKER_TAG=latest

# --- Misc toggles ---
# METRICS_ENABLE=true
# HEALTH_ENDPOINT=/healthz

# ======================================================
# Notes:
# 1. Remove DB_DSN if you prefer constructing it from parts.
# 2. Ensure JWT_SECRET and DB_PASSWORD are kept secret (use Docker/K8s secrets).
# 3. Adjust sslmode in DB_DSN for production security.
# 4. Set GIN_MODE=release to improve performance.
# ======================================================
