# My .env for be03 project

# --- Database ---
# Postgres DSN format:
#   postgres://user:password@host:port/dbname?sslmode=disable
# or the libpq style:
#   host=localhost user=postgres password=secret dbname=test port=5432 sslmode=disable
DB_DSN=postgres://postgres:postgres@localhost:5432/cat_keu?sslmode=disable

# (Optional explicit parts – override DSN build in code / docker-compose)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=cat_keu

# --- Security ---
# JWT secret used to sign tokens (CHANGE THIS before production!)
JWT_SECRET=devsecret

# --- Server ---
# Port your Go service will listen on
SERVER_PORT=8081

# Allow CORS (comma-separated list of allowed origins)
# Example: http://localhost:3000,http://127.0.0.1:5173,https://yourdomain.com
ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# --- Environment ---
# dev, staging, prod
APP_ENV=dev

# --- Logging ---
# debug, info, warn, error
LOG_LEVEL=debug

# --- Deployment / Docker (development defaults) ---
# Image metadata (used by build scripts / CI if added later)
DOCKER_IMAGE=keu-app
DOCKER_TAG=dev

# Public base URL (used for CORS / absolute links)
PUBLIC_BASE_URL=http://localhost:${SERVER_PORT}

# Toggle to force Gin release mode when needed
# GIN_MODE=release

# ================= Production Guidance =================
# DO NOT commit real secrets. Create a separate .env.production file (see .env.production.example).
# For production at keu.fardil.com you will likely set:
#   APP_ENV=prod
#   LOG_LEVEL=info
#   SERVER_PORT=8080 (or 80 inside container)
#   PUBLIC_BASE_URL=https://keu.fardil.com
#   ALLOW_ORIGINS=https://keu.fardil.com
#   JWT_SECRET=<<strong-random-64+ chars>>
#   DB_HOST=your-managed-postgres-host
#   DB_PORT=5432
#   DB_USER=keu_app
#   DB_PASSWORD=<<strong-password>>
#   DB_NAME=cat_keu
#   DB_DSN=postgres://keu_app:<<strong-password>>@your-managed-postgres-host:5432/cat_keu?sslmode=verify-full
# =======================================================

# --- Optional: Third-party services ---
# If you later use S3, GCP, or external APIs, add them here.
# S3_BUCKET=my-bucket
# S3_REGION=ap-southeast-1
# S3_ACCESS_KEY=xxxx
# S3_SECRET_KEY=xxxx
